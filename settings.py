# read_pdf.pyの設定
# 処理を行う最初のページ（pdfviewerで確認した値）-①
first_page: int = 17

# 処理を行う最初のページに記載されているページ数（pdfの画像内にある値）-②
first_page_in_book: int = 9

# 処理を行う最後のページ（pdfviewerで確認した値）-③
last_page: int = 504

# pdfのパス
pdf_path: str = "book.pdf"

# search_split_point_candidate.pyの設定
# 検索する文字サイズの最小値 -④
min_font_size: int = 10
# 検索する文字サイズの最大値 -⑤
max_font_size: int = 100


# create_prompt.pyの設定
# プロンプトに一度に入力するぺージ数 -⑥
page_count: int = 12
# 入力する命令 -⑦
order: str = '''# 入力
{text}

あなたはプロの編集者です。
以下の制約条件に従って、上記の文章を箇条書きで要約してください。
# 制約条件
・架空の表現や言葉を使用しない。
・文章中の数値には変更を加えない。
・各要素に対応する文章の頭のページ番号を箇条書きの末尾につける。
・「だ・である調」で要約する。
・体言止めをしない。
・全てのページについて、1ページにつき1つ以上は要素を書き出す。
・なるべく固有名詞を拾うようにする。

# 出力例
 - マスコミはこれらの本をまともなものとして扱っていた (p.25)
 - 1987年12月27日、テレビ東京は「国際ユダヤ資本家」のドキュメンタリーを全国放映 (p.25-26)

# 出力
以下に要約を箇条書きで作成した。:
 - 
'''

# summary.pyの設定
# temperatureの設定
temperature: float = 0.1
# top_pの設定
top_p: float = 0.99
# max_tokensの設定
max_tokens: int = 3900
# 実行時のsleep時間 -⑧
sleep_time: int = 1
# 実行時に同時に処理する数 -⑨
worker_count: int = 2

# summary_at_once.pyの設定 -⑩
file_name: str = "0000.txt"

# refine_summary.pyの設定
# リファイン時の出力上限トークン数（章まるごと出力するため大きめに）
refine_max_tokens: int = 6000
# リファイン用プロンプト
refine_order: str = '''あなたはプロの編集者です。下記の「章原文」と「現在の要約」を読み、要約を完成形へ磨いてください。

# 制約
1. 章の主題に直結する項目を残し、脱線的な固有名詞列挙や著者のエッセイ的描写・自伝的回想を削除する（目安：項目数の6～8割を残す。）。
2. 内容が非常に近い複数の項目のみ1つの自然な文に統合する。
3. 人物の肩書きや関係性が文脈で必要な場合は短く補足する（例：「ハリファックス」→「外相ハリファックス」）。
4. 章原文に照らし、固有名詞・地名・用語の誤字や誤訳を修正する。
5. 「以下に要約を箇条書きで作成した。」のような生成過程のメタ文字列が混入していたら必ず削除する。
6. 各項目末尾のページ番号 (p.X) または (p.X-Y) は元の表記を維持する。
7. 数値は変更しない、架空の表現は使わない。
8. 「だ・である調」で、体言止めをしない。

# 出力フォーマット（厳守）
- 行頭は全角スペース「　」1つでインデントする（「- 」や「・」は付けない）。
- 各項目は1行に収め、行末には半角スペース2個を付ける（Markdown改行）。ただし最終行のみ半角スペースを付けない。
- 見出し行（# で始まる行）や前置き・後書きは出力しない。箇条書きの本文だけを出力する。

# 出力例（フォーマットのみ参考、内容ではなく形を真似ること）
　1940年5月28日の午後、イギリスの戦時内閣メンバー7人がウェストミンスター宮殿の下院に集まり、国の存亡にかかわる問題について議論していた (p.18)
　チャーチルはハリファックスの提案に否と答え、イギリスをこうした立場に置いてはいけないと考えていた (p.25)

# 章原文
{chapter_source}

# 現在の要約（before）
{chapter_before}

# 改善した要約（after）
'''